# Runtime dependencies — only what serving the model needs.
# For training and evaluation, use requirements-dev.txt instead.

# Web
flask>=3.0.0
flask-cors>=4.0.0
gunicorn>=21.2.0

# Inference
# tensorflow-cpu skips the ~1.5GB of nvidia/CUDA wheels that plain
# `tensorflow` pulls in on Linux and that no CPU host can use anyway.
# Pinned to 2.15 (Keras 2.x): emotion_model.h5 was saved with Keras 2.13
# and TensorFlow >= 2.16 ships Keras 3, which cannot load that legacy .h5.
tensorflow-cpu==2.15.1

# opencv-python-headless drops the GUI/Qt bindings the server never calls.
opencv-python-headless>=4.6.0
numpy>=1.23.0,<2.0.0
